'use strict';

const calenderReact = require('..');
const assert = require('assert').strict;

assert.strictEqual(calenderReact(), 'Hello from calenderReact');
console.info("calenderReact tests passed");
